{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "auth": "turbo run dev_pkg --filter=./packages/auth --no-daemon",
    "ui": "turbo run dev_pkg --filter=./packages/ui --no-daemon",
    "dev": "turbo run dev --no-daemon",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "botswork",
  "dependencies": {
    "g": "^2.0.1",
    "pnpm": "^8.6.12"
  }
}
