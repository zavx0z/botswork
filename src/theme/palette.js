import {createTheme} from "@mui/material/styles"

export const themeColor = createTheme({
    palette: {
        primary: {
            main: '#0c1f3c',
        },
        secondary: {
            main: "#baddf9"
        }
    },
})